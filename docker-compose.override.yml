version: '3.9'

services:
  csk-api:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    volumes:
      - ./src:/app/src
    environment:
      - DOTNET_WATCH_RESTART_ON_RUDE_EDIT=true
